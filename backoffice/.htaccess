# RewriteEngine On

# Redirige les URL contenant le paramètre ?i=1
# RewriteCond %{QUERY_STRING} (^|&)i=1(&|$)
# RewriteRule ^(.*)$ /$1? [R=301,L]

# Add Basic Auth into the App
AuthType Basic
AuthName "Restricted Area"
AuthUserFile .htpasswd
Require valid-user